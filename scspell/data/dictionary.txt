if (str.charAt(count) == curr.data)
                {
                    if (count == 0)
                        index = i;
                    count ++;
                }
                else if (count < str.length())
                {
                    index = 0;
                    count = 0;
                }
                curr = curr.next;
            }
            return -1;
